# =============================================================================
# PSPD Guardian — Docker Environment Configuration
#
# Copy this file to .env and customize values for your deployment:
#   cp .env.docker .env
#
# =============================================================================

# ---- PostgreSQL ----
PG_DB_NAME=guardian_db
PG_DB_USER=guardian_user
PG_DB_PASSWORD=guardian_pass
PG_EXTERNAL_PORT=5432

# ---- Qdrant ----
QDRANT_COLLECTION=guardian_incidents
QDRANT_EXTERNAL_PORT=6333

# ---- Ollama (Remote LLM Server) ----
# Point this to your Ollama instance.
# The container does NOT run Ollama — it connects to this external server.
OLLAMA_BASE_URL=http://31.220.21.156:11434
OLLAMA_MODEL=llama3.1:8b

# ---- Django ----
DJANGO_SECRET_KEY=change-me-in-production-use-a-long-random-string
DJANGO_DEBUG=False

# ---- Gunicorn ----
GUNICORN_WORKERS=2
GUNICORN_TIMEOUT=300

# ---- Ports ----
BACKEND_EXTERNAL_PORT=8001
FRONTEND_EXTERNAL_PORT=3000
PROXY_PORT=8080

# ---- Frontend ----
# This is the URL the browser uses to reach the backend API.
# In production, set this to your public domain:
#   REACT_APP_BACKEND_URL=https://guardian.yourdomain.com
# For local Docker deployment, use the proxy port:
REACT_APP_BACKEND_URL=http://localhost:8080
